name: CI

on:
  push:
    branches:
      - "*"
      - "!master"

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Lint code
        shell: pwsh
        run: ./.github/scripts/Assert-Linting
      - name: Install Dependencies
        shell: pwsh
        run: ./.devcontainer/Install-Dependencies
      - name: Test code
        shell: pwsh
        run: ./.github/scripts/Assert-Tests